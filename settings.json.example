{
  "_comment": "Session Manager Configuration",
  "_comment": "This file allows you to configure the session manager type and its options.",
  "_comment": "Valid types: 'tmux' (default) or 'process'",

  "sessionManager": {
    "type": "tmux",

    "_comment_tmux": "Configuration for tmux session manager (used when type: 'tmux')",
    "tmux": {
      "prefix": "pi_project_"
    },

    "_comment_process": "Configuration for process session manager (used when type: 'process')",
    "_comment_process": "This allows you to run any command instead of tmux",
    "process": {
      "_command_command": "The command to run (e.g., 'pi', 'npm', '/usr/bin/python')",
      "_command_example": "command: 'pi'",
      "command": null,

      "_args_comment": "Array of arguments to pass to the command",
      "_args_example": "args: ['--project', '${projectName}', '--issues', '${issueCount}']",
      "_args_placeholders": "Available placeholders: ${projectName}, ${projectId}, ${sessionId}, ${issueCount}",
      "args": [],

      "prefix": "pi_project_"
    }
  }
}