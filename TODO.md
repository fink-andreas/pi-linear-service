# TODO - INN-201 installable pi package

- [x] Add pi package manifest in `package.json` and include extension resources in published files
- [x] Create package extension resource layout under `extensions/`
- [x] Update README with explicit `pi install` global/local flows
- [x] Run verification (`npm test` and `LINEAR_API_KEY=test ASSIGNEE_ID=test node index.js`)
